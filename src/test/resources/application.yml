spring:
  flyway:
    enabled: true
  datasource:
    url: "jdbc:h2:mem:veilarbregistrering;DB_CLOSE_DELAY=-1;MODE=Oracle"
    username: "sa"
    password: "password"
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
