query($ident: ID!) {
    hentIdenter(ident: $ident, grupper: [FOLKEREGISTERIDENT, AKTORID], historikk: true) {
        identer {
            ident,
            historisk,
            gruppe
        }
    }
}