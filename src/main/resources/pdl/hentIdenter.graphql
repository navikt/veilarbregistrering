query($ident: ID!) {
    hentIdenter(ident: $ident, grupper: [FOLKEREGISTERIDENT, AKTORID], historikk: false) {
        identer {
            ident,
            historisk,
            gruppe
        }
    }
}