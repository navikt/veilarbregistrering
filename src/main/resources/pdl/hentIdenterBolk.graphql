query($identer: [ID!]!) {
    hentIdenterBolk(identer: $identer, grupper: [FOLKEREGISTERIDENT], historikk: true) {
       ident,
       identer {
           ident,
           historisk
       },
       code
   }
}